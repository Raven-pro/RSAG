# RSAG 课题组网站 - 本地测试指南

## 🚀 快速开始

### 前提条件
- 安装 [Node.js](https://nodejs.org/) (推荐 LTS 版本)
- 确保 npm 已随 Node.js 安装

### 启动步骤

#### macOS/Linux 用户:
```bash
# 1. 进入项目目录
cd /Users/providence/Project_dev/RSAG

# 2. 赋予启动脚本执行权限
chmod +x start-local.sh

# 3. 启动本地服务器
./start-local.sh
```

#### Windows 用户:
双击运行 `start-local.bat` 文件

#### 手动启动:
```bash
# 1. 安装依赖
npm install

# 2. 启动服务器
npm start
```

## 🌐 访问地址

启动成功后，您可以访问：

- **前端网站**: http://localhost:3000/index.html
- **管理后台**: http://localhost:3000/admin/login.html

## 👤 测试账号

### 管理员账号
- 用户名: `admin`
- 密码: `rsag2025!`
- 权限: 完全管理权限

### 编辑员账号
- 用户名: `editor`
- 密码: `rsag_edit2025`
- 权限: 内容编辑权限

## 🎯 功能测试

### 1. 登录测试
1. 访问 http://localhost:3000/admin/login.html
2. 使用管理员账号登录
3. 成功后会跳转到管理仪表板

### 2. 论文管理测试
1. 点击侧边栏 "📚 论文管理"
2. 尝试添加新论文
3. 编辑现有论文
4. 测试搜索功能

### 3. 新闻管理测试
1. 点击侧边栏 "📰 新闻管理"
2. 发布新新闻
3. 使用预览功能
4. 测试富文本编辑

### 4. 团队管理测试
1. 点击侧边栏 "👥 团队管理"
2. 添加新成员
3. 上传头像（模拟功能）
4. 调整显示顺序

### 5. 文件管理测试
1. 点击侧边栏 "📁 文件管理"
2. 模拟文件上传
3. 查看文件详情
4. 复制文件链接

## 📊 数据说明

### 本地测试数据
- 所有数据存储在内存中（`local-server.js`）
- 服务器重启后数据会重置
- 包含一些示例数据便于测试

### 真实部署环境
- 数据存储在 Cloudflare D1 数据库
- 数据持久化保存
- 支持多用户并发访问

## 🔧 开发调试

### 启用开发模式
```bash
# 安装 nodemon (自动重启)
npm install -g nodemon

# 启动开发模式
npm run dev
```

### 查看日志
服务器会在控制台显示所有 API 请求和错误信息

### 修改端口
如果 3000 端口被占用，可以修改 `local-server.js` 中的 `PORT` 变量

## 🌟 与前端网站的集成

### 测试前端页面
1. 访问 http://localhost:3000/index.html
2. 查看是否正常显示
3. 测试中英文切换
4. 测试响应式设计

### 数据同步测试
1. 在管理后台添加论文/新闻
2. 理论上前端会实时更新（需要额外开发）
3. 当前版本需要手动刷新页面

## 🚨 常见问题

### Q: 无法启动服务器
A: 检查是否安装了 Node.js，端口是否被占用

### Q: 登录失败
A: 确认用户名密码正确，检查网络连接

### Q: 文件上传失败
A: 本地版本为模拟上传，会返回模拟的文件路径

### Q: 数据丢失
A: 本地测试数据存储在内存中，重启后会重置

## 📝 下一步计划

### 本地完善
- [ ] 添加文件实际上传功能
- [ ] 实现数据持久化（SQLite）
- [ ] 添加更多测试数据

### 部署准备
- [ ] 配置 Cloudflare D1 数据库
- [ ] 设置 Cloudflare Pages
- [ ] 配置环境变量
- [ ] 测试生产环境

## 💡 技术说明

### 本地服务器特性
- Express.js 框架
- CORS 支持跨域请求
- JWT 认证（简化版本）
- RESTful API 设计
- 静态文件服务

### 与 Cloudflare 的差异
- 本地使用内存存储，Cloudflare 使用 D1 数据库
- 本地使用简化 JWT，Cloudflare 使用完整 JWT
- 本地无文件上传，Cloudflare 使用 R2 存储

---

## 🎉 开始测试

运行启动脚本后，打开浏览器访问管理后台开始测试吧！

如有问题，请检查控制台输出或联系开发团队。
